import 'dart:ui';

class ContactUsListModel {
  String title;
  String image;
  Color backgroundColor;
  Color borderColor;
  ContactUsListModel(
      this.title, this.image, this.borderColor, this.backgroundColor);
}
